<%= form_with(model: curso, local: true) do |form| %>
  <% if curso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(curso.errors.count, "error") %> prohibited this curso from being saved:</h2>

      <ul>
      <% curso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label :numero %>
    <%= form.number_field :numero, id: :curso_numero %>
  </div>

  <div class="field">
    <%= form.label :grado %>
    <%= form.text_field :grado, id: :curso_grado  %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: tipo_infraccion, local: true) do |form| %>
  <% if tipo_infraccion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_infraccion.errors.count, "error") %> prohibited this tipo_infraccion from being saved:</h2>

      <ul>
      <% tipo_infraccion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :tipo_infraccion_nombre %>
  </div>

  <div class="field">
    <%= form.label :monto %>
    <%= form.number_field :monto, id: :tipo_infraccion_monto %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
